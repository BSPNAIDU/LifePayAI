version: '3.8'

services:

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

    container_name: lifepayai-backend

    ports:
      - "5001:5000"

    env_file:
      - ./backend/.env

    restart: always